{{#each beersList}}
    {{> beerpartial this}}
    {{!-- <h2>{{this.name}}</h2>
    <img src="{{this.image_url}}" alt="{{this.image_url}}">
    <p>{{this.description}}</p>
    <p>{{this.tagline}}</p> --}}
{{/each}}