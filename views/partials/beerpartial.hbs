   <a href="{{this.id}}">
      <img class="beerCardImage" src="{{this.image_url}}" alt="{{this.image_url}}"> 
   </a>
   <div class="textbelowImage">
         <h5>{{this.name}}</h5>
         <p class="tagline">{{this.tagline}}</p>
         <p>{{this.description}}</p>
   </div>
